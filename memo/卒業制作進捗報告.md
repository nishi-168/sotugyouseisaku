# イベント一覧ページ（仮）

## 調査・学習した内容

### Prismaのincludeの使い方

休んでいた時の範囲だったのでわからなかった部分を調べた。

### Prisma Clientを使い回すためのシングルトンの接続方法

最初AIに教えてもらった方法はパスを何回も通す方法だったのでエラーになっていたが、lib/prismaを通してシングルトンにしていたことを思い出して解決した。

### Prismaのスキーマ変更時の更新方法

スキーマ修正 → migrate（テーブル構造変更時） → generate（型の情報の更新）という一連の操作が必要なこと。

### seed更新方法

seedを変更したときは、以下のコマンドでseedを再実行する必要がある。

```bash
pnpm prisma db seed
```

## 悩み・疑問・課題

* Prismaの新しいバージョンでの接続方法が違うらしい
* Prismaの_countはまだよくわかっていない

## 設計

* イベント一覧にはDBに登録されているデータを全て表示させる
* 参加人数はテーブルに値を持たせず計算することにした

## 仕様

* **表示項目**：場所、カテゴリ名、主催者名、参加人数（現在数/募集人数）
* **並び順**：イベント作成日時の降順（新着順）
* **データ取得方法**：PrismaのfindManyにinclude（カテゴリ・主催者）と_count（参加人数集計）を指定して1回のクエリで取得

    
# イベント詳細ページ

## 調査・学習した内容

### Next.jsのルーティング

`[id]`によるルートパラメータの実装。

### PrismaのfindUniqueとfindManyの使い分け

`findUnique`は指定した1件だけを取得したい場合に使用する。

### 原因不明のランタイムエラーが出た時の解消方法

AIに聞いても不明だったので、コードを切り分けながら追加していって原因を特定した。

結局、完成したコードをもう一度貼り付けたらなぜか治った。

### notFound()関数によるエラーハンドル

`notFound()`関数を使用して、存在しないデータにアクセスした場合に404ページを表示する方法。

### deleteManyだけでは自動採番がリセットされないこと

`deleteMany`だけでは自動採番がリセットされないため、開発中にIDのズレが起こりうること。

## 設計

* 存在しないイベントIDはエラー画面ではなく404画面で処理する方針
* 画面遷移はLinkコンポーネントを使用して、ページ全体の再読み込みを避けた

## 仕様

* **URL形式**：`/participant/[イベントID]`
* **表示項目**：イベント名、場所、日時、カテゴリー名、主催者名、詳細、申込期限、参加人数（現在数/募集人数）
* **存在しないIDへのアクセス時**：404ページを表示
* **一覧ページの各イベント項目**：リンク化し、クリックで対応する詳細ページに遷移する


# ユーザー識別（擬似ログイン）機能

ログイン機能を使わず、メールアドレスによる検索でユーザーを識別する擬似ログイン画面の実装。

## 調査・学習した内容

### Next.jsの`use server`によるフォーム送信処理の実装方法

`use server`を使うことで、フォーム送信時にAPIを用意せずサーバー側で処理するようにできる。

`use server`はファイルの一番先頭に書く。

### `cookies()`関数を用いたCookieの読み書き方法

パスワード認証を使わずとも、Next.jsのライブラリで擬似ログインをすることができる。

### `redirect`の使い方

ブラウザを指定したページに移動させるもの。

## 設計

* パスワードは使わない擬似ログイン機能を実装
* UniqueなメールアドレスをIDと紐づけることでユーザーを識別する
* 検索に成功した場合はユーザーIDをCookieに保存して、以降のページ操作でユーザーを識別して扱うことができる
* 該当ユーザーがいない場合はログイン画面にエラー付きメッセージを出す

## 仕様

* **画面**：`/login`にメールアドレス入力フォームを表示
* **検索成功時**：Cookieに`userId`を保存し、`/participant`（イベント一覧）へ遷移
* **検索失敗時**：`/login?error=notfound`へ遷移し、「該当するユーザーが見つかりませんでした」と表示

# 参加申込み機能実装

## 調査・学習した内容

### `prisma.$transaction`を用いた複数DBの一括実行

まだ理解が浅いため要復習。

### Server Actionでの複数条件によるバリデーション

これはアプリ側でもいいのでは？勢いで作ってしまった。

### URLのクエリパラメータを使った処理結果画面への伝達方法

処理の成功・失敗などの結果をURLのクエリパラメータで別のページに伝える方法。

### Cookieについて

Cookieはブラウザ側に保存されるデータであり、サーバーを再起動しても保持される。

## 悩み・疑問

### 参加ボタンを押すとログインページに戻されてしまうバグがあった

スペルミスが原因だった。

### Cookieが残ってしまう問題

挙動テストをしたかったが、Cookieが残ってしまい続けるので、早めにログアウト機能を作りたい。

## 設計

参加処理はServer Action内で以下の順にチェックを行う設計とした。

**ログイン確認 → イベント存在確認 → 主催者本人チェック → 定員チェック → 参加登録**

画面側（ボタンの`disabled`）とサーバー側（Server Action内の判定）の両方で主催者チェック・定員チェックを行う二重の設計とし、画面操作を回避した不正な参加登録を防ぐようにした。

処理結果（成功・失敗の理由）はURLのクエリパラメータで詳細ページに伝え、対応するメッセージを表示する設計とした。

## 仕様

* **ログインしていない状態で参加ボタンを押す**：`/login`にリダイレクトされる
* **存在しないイベントIDの場合**：`?error=notfound`とともに詳細ページに戻る
* **自分が主催するイベントの場合**：参加ボタンの代わりに「自分が主催するイベントです」と表示する
* **定員に達している場合**：参加ボタンが非活性になり「満員です」と表示する。裏側で定員に達していた場合も同様のエラーメッセージを表示する
* **参加成功時**：`Participation`レコードの作成と`User`の`participationCount`の加算を同時に行い、「参加申込みが完了しました!」と表示する


# ログアウト機能・共通ヘッダー作成

## 調査・学習した内容

### Next.jsの`layout.tsx`の役割

`layout.tsx`は全ページ共通の外枠を定義するファイルであり、ヘッダーやフッターなど、どのページでも共通で表示させたいものがある時に使える。

### Cookieを削除する方法

Cookieを削除する方法。

ほんとに消えてんの？

### フラグメントの使いかた

2つ以上のものを塊として渡したい時に使う。

### レイアウトの一部だけを`async`にすること

レイアウト全体を非同期にするのではなく、必要な箇所だけCookieを読み取り、DB検索をしてから表示するといった非同期処理を使える。

## 設計

* トップページ（`/`）へのアクセスは、ログイン状態にかかわらず一律で`/participant`（イベント一覧）へ案内する設計に変更した
* ヘッダーは常時表示し、ログイン状態に応じて「ようこそ〇〇さん＋ログアウトボタン」または「ログインへのリンク」のいずれかを表示する設計とした
* 参加者タブ・主催者タブへのリンクは、ログイン状態にかかわらず常にヘッダーに表示する設計とした（イベント一覧・検索は誰でも見られる要件のため）
* ログイン確認はサイトに入るための条件ではなく、特定の操作（現段階はイベントへの参加）を行うときだけにすることで、イベント一覧は誰でも見ることができる

## 仕様

* **`/`にアクセスすると**：ログイン状態にかかわらず`/participant`に遷移する
* **ヘッダー**：常に「参加者タブ」「主催者タブ」へのリンクを表示する
* **ログイン済みの場合**：「ようこそ、〇〇さん」の表示とログアウトボタンを表示する
* **未ログインの場合**：「ログイン」へのリンクを表示する
* **ログアウトボタンを押すと**：Cookieの`userId`が削除され、`/login`に遷移する


# ユーザー登録機能実装

## 調査・学習した内容

### Yupの復習

最初AIには`if`を勧められていたが、Yupは習っていたし、コードが見やすくなりそうだったためYupを採用した。

### ブラウザ側とサーバー側のバリデーションチェック

二重チェックをすることで、より安全な入力チェックになった。

### `"use server"`を使った時のデバッグ

アプリ側にログが出ないので、`console.log`などでターミナルを確認してエラーを特定する必要があること。

### フォームの入力欄を複数に分割している場合のバリデーション

フォームの入力欄を複数に分割している場合（年・月・日など）、サーバー側で1つの値に組み立て直してからバリデーションにかける必要があること。

## 悩み・疑問だった点

* URLエンコードについてまだよくわかってない
* あとYup
* デバッグの段階でバリデーションを通っているはずなのにうまく登録できない問題があり、それを直すのに時間を使ってしまった
* コメントアウトなどで切り分けることで原因を特定し、バリデーションはうまく動いていたことがわかった
* 生年月日の登録方法を色々考え、試行錯誤した

## 設計

* ユーザー登録の入力チェックは、ブラウザ標準の属性（HTML側）とYup（サーバー側）の二段構えとする方針とした
* 生年月日は、扱いやすさを優先してプルダウンではなく、年・月・日をそれぞれ数値で直接入力する方式を採用した
* 登録完了と同時にCookieに`userId`を保存し、そのままログイン状態として`/participant`に遷移する設計とした（再度ログイン画面での検索操作を不要にするため）

## 仕様

* **入力項目**：名前、生年月日（年・月・日を個別入力）、電話番号、メールアドレス
* **電話番号**：ハイフンあり・なしどちらの形式でも登録可能
* **メールアドレス**：形式チェックに加え、既存ユーザーとの重複チェックを行う
* **入力エラーがあった場合**：該当するエラーメッセージを登録画面に表示する
* **登録成功時**：自動的にログイン状態となり、イベント一覧画面（`/participant`）に遷移する
